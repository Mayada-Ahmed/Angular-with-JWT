export interface product {
    id: number;
    name: string;
    description: string;
    image: string
}

export interface createProduct{
    name: string;
    description: string;
    image: string
}